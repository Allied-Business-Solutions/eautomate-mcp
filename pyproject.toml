[project]
name = "eautomate-mcp"
version = "0.1.0"
description = "MCP server bridging Claude and eAutomate PublicAPI"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]<2.0",
    "zeep",
    "requests",
    "python-dotenv",
    "pydantic",
]
